server:
  port: 9002 # 로컬에서 띄울 포트

spring:
  config:
    activate:
      on-profile: "local"
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: nhnacademy123!

  application:
    name: member-server

  datasource:
    url: jdbc:mysql://localhost:3306/member_db?serverTimezone=Asia/Seoul
    username: root
    password: 123123123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always
  data:
    redis:
      #      host: 220.67.216.14
      host: 10.116.64.14
      port: 6379
      password: '*N2vya7H@muDTwdNMR!'
      database: 19

app:
  rabbitmq:
    enabled: false

management:
  health:
    rabbit:
      enabled: false

eureka:
  client:
    enabled: true
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    root: info

jwt:
  secret: "v3RyS3cr3tK3yF0rJwT5ign1ngPurp0s3sOnlyD0N0tSh4r3Th1s"
  expiration_time: 5000
  refresh_expiration_time: 10000


springdoc:
  version: V.0.0.1
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /api-docs

  default-produces-media-type: application/json;charset=UTF-8